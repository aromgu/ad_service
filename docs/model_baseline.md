# 모델 개발 베이스라인 실행서

## 팀 일정 기준

팀장 일정표의 “모델 개발 / 박창준” 행을 기준으로 하되, 데이터셋 구축은 보류하고 같은 입력으로
여러 모델을 비교한다는 팀 결정을 반영한다.

| 날짜 | 베이스라인 산출물 | 현재 상태 |
| --- | --- | --- |
| 9/1 | 데이터 후보와 이용 조건 조사 | 완료 |
| 9/2 | 공통 입력 구조, GPU Docker, Qwen3-8B 1건 스모크 테스트 | 완료 |
| 9/3 | 세 입력 모드와 사용자 문구 스타일을 지원하는 Baseline 코드 | 진행 중 |
| 9/4 | 로컬·SSH L4 실행 절차와 실패 로그 정리 | 진행 중 |
| 9/7 | 동일 요청으로 문구·이미지 후보 모델 실행 | 예정 |
| 9/8 | 문구·이미지 평가표와 비용·속도 측정 | 예정 |
| 9/9 | 베이스라인 결과로 QLoRA 필요 여부 결정 | 예정 |
| 9/10 | 승인된 경우에만 Qwen3-8B QLoRA 실행 | 조건부 |
| 9/11 | 모델별 품질·속도·비용·실패 원인 비교 | 예정 |
| 9/14 | 프롬프트·전처리·폴백 개선 | 예정 |
| 9/15 | 1차 학습 개선안 재실험 | 조건부 |
| 9/16 | API 모델과 L4 모델 동일 요청 비교 | 예정 |
| 9/17 | 기본 모델과 폴백 후보 선정 | 예정 |
| 9/18 | 서빙용 함수·CLI·JSON 계약 고정 | 예정 |
| 9/21 | 실패 유형과 재시도 결과 정리 | 예정 |
| 9/22 | 합격 기준으로 최종 평가 | 예정 |
| 9/23 | 메모리·속도·배치 설정 최적화 | 예정 |
| 9/24 | 팀 리뷰를 거쳐 모델 결정 | 예정 |
| 9/25 | 구아롬에게 실행법·입출력 예시 전달 | 예정 |
| 9/28 | 서비스 연동 환경 회귀 테스트 | 예정 |
| 9/29 | 남은 제한사항과 후속 작업 기록 | 예정 |
| 9/30 | 배포 대상 버전과 체크섬 고정 | 예정 |
| 10/1 | 통합 시스템에서 필수 산출물 검증 | 예정 |
| 10/2 | 발표용 결과·평가표·실패 사례 정리 | 예정 |

## 완료 기준

- 텍스트만·이미지만·텍스트와 이미지 입력에서 요청한 결과만 생성한다.
- 문구 모델 3개, 이미지 모델 2개를 동일 입력으로 비교한다.
- JSON 파싱 성공률 100%, 오류와 비용은 `experiment.jsonl`과 `budget.json`에 남긴다.
- 제품 보존 점수 4/5 미만과 확인되지 않은 상품 주장은 탈락시킨다.
- 사용자가 문구 스타일·길이·필수 표현·제외 표현을 지정할 수 있어야 한다.

## 실행 순서

1. mock 공급자로 세 입력 모드가 동작하는지 확인한다.
2. 동일한 문구 요청을 GPT-5.4 Mini/Nano와 Qwen3-8B에 각각 실행한다.
3. 동일한 이미지 요청을 GPT-Image-2와 FLUX.2 Klein 4B에 각각 실행한다.
4. 문구 스타일과 길이 옵션을 바꿔 지시 준수 여부를 비교한다.
5. 필요할 때만 추가 시드를 사용해 이미지 결과의 안정성을 확인한다.
6. 평가표를 만들고 박창준·황인홍이 독립적으로 점수를 입력한다.
7. 모델별 평균을 집계한다. 3점 이내 동점이면 비용, 지연 순으로 선택한다.

## 비용 계획

- 코드의 사전 추정치는 지원 규격별 공개 단가를 사용한다.
- 실행 전후 팀 채널의 `!usage`로 실제 팀 사용량도 확인한다.
- 프로젝트 코드의 누적 상한은 $10, 팀 전체 상한은 $30이다.

## 결과 전달

구아롬에게는 `GenerationRequest`, `GenerationResult`, `/v1/generate`와 환경 변수 목록을 전달한다.
이태훈에게는 각 `result.json`과 PNG 3종, `text_safe_area` 좌표를 전달한다. 한글 최종 렌더링은
서비스의 HTML/Canvas로 교체해도 결과 스키마는 유지한다.

## 직접 학습 판단

이번 베이스라인에서는 파인튜닝하지 않는다. 직접 학습이 필수이거나 반복되는 한국어 카피 실패가
프롬프트로 해결되지 않고 검수 문구 500쌍 이상이 확보된 경우에만 Qwen3-8B QLoRA를 별도 단계로
진행한다. 데이터의 다양성과 이용 허가가 확보되기 전에는 이미지 LoRA를 진행하지 않는다.
